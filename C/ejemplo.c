#include <stdio.h>
#include <stdlib.h>

typedef struct NODO
{   
    int dato;
    struct NODO *hijos;
}nodo_t;


int main(int argc, char const *argv[])
{
    nodo_t *raiz = malloc(sizeof(struct NODO));

    raiz->hijos= malloc(sizeof(struct NODO)* 3);

    raiz-> hijos = malloc(sizeof(struct NODO));

    raiz->hijos+i->dato=3;
    
    return 0;
}
